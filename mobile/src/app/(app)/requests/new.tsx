import { router } from 'expo-router'; import { ScrollView, StyleSheet, Text } from 'react-native'; import { RequestForm } from '@/components/RequestForm'; import { ScreenContainer } from '@/components/ScreenContainer'; import { createRequest } from '@/services/request.service'; import { colors, spacing, typography } from '@/theme';
export default function NewRequestScreen(){return <ScreenContainer><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><Text style={styles.title}>Nova solicitação</Text><RequestForm submitLabel="Criar solicitação" onSubmit={async input=>{const created=await createRequest(input);router.replace(`/requests/${created.id}`);}}/></ScrollView></ScreenContainer>}
const styles=StyleSheet.create({content:{gap:spacing.lg,paddingBottom:spacing.xl},title:{color:colors.text,fontSize:typography.title.fontSize,fontWeight:'700'}});
